{#methods obj_name=name}
JSBool def_{name}_{.}(JSContext, uintN, jsval);
{/properties}
static JSFunctionSpec functions[] = {
	{#methods obj_name=name}
		{
			"{.name}", 
			(JSNative)def_{obj_name}_{.name},
			{.argCount},
			JSFUN_FAST_NATIVE,
			0
		},
	{/properties}
};
