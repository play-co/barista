{#methods obj_name=name}
JSBool def_{name}_{.name}(JSContext, uintN, jsval);
{/methods}
static JSFunctionSpec functions[] = {
	{#methods obj_name=name}
		{
			"{.name}", 
			(JSNative)def_{obj_name}_{.name},
			{.argCount},
			JSFUN_FAST_NATIVE,
			0
		},
	{/methods}
		{0}
};

